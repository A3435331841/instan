# GRT-360 交接文档（2026-08-25 23:35 CST）

本文档用于紧急交接当前 GRT-360 训练/评估任务。当前任务还没有完成：尚未找到同时满足 `AUC > 0.8`、`SR > 0.8`、`FPS > 30` 的可提交候选。

## 1. 一句话状态

远端机器运行正常，当前 6 条 medium 验证 + 1 条 representative 验证在线推进；无 OOM、Traceback、Killed；当前仍处于“补齐调研方案证据”阶段，不能进入最终融合，也不能提交。

最新审计时间：`2026-08-25 23:35:47 CST`

## 2. 不能忘的硬约束

- 最终停止条件：必须有经验证的方案满足 `AUC > 0.8`、`SR > 0.8`、`FPS > 30`。
- 不要把代表性小集、partial medium 或并发 FPS 当成最终结论。
- 不要因为当前结果低于 0.8 就一刀切丢方案；接近现有速度/精度基线、或在某类序列上有专家价值的方案都要保留到融合前分析。
- 不要在 `ft_ep2/3/4` representative 和当前 medium 覆盖补齐前做最终融合。
- 不要执行 `docker push`，除非用户明确确认要消耗提交配额。
- 不要在日志、文档、终端输出中写出 SSH 密码或其他凭据。

## 3. 远端与本地环境

远端当前使用实例：`p-10466`

- 主机：`36.133.158.119`
- SSH 端口：`55004`
- 用户：`wuyou`
- 密码：不写入本文档；如需连接，从用户安全渠道获取，或使用本地已配置的 `tools_local/srv.py` 环境变量方式。

本地项目路径：

- `D:\instan\pano360`

远端关键路径：

- 远端项目：`/data/pano360`
- 训练数据：`/data/traindata/train`
- Python：`/home/wuyou/grt_env/bin/python`
- representative 结果：`/data/runs/representative_leaderboard_20260825`
- medium 结果：`/data/runs/medium_validation_20260825`
- medium 汇总：`/data/runs/medium_validation_20260825/medium_summary.md`
- component 诊断：`/data/runs/medium_validation_20260825/component_diagnostics.md`
- potential 矩阵：`/data/runs/representative_leaderboard_20260825/potential_matrix.md`

本地连接建议命令骨架：

```powershell
Set-Location D:\instan\pano360
$env:PYTHONIOENCODING='utf-8'
$env:GRT_SSH_HOST='36.133.158.119'
$env:GRT_SSH_PORT='55004'
$env:GRT_SSH_USER='wuyou'
$env:GRT_SSH_PASS='<从安全渠道获取，不要写入日志>'
python tools_local\srv.py cmd '<remote bash command>' 180
```

## 4. 当前运行状态

最新审计：

- GPU0：RTX 3090，利用率约 `90%`，显存 `6263 / 24576 MiB`，温度 `71°C`
- GPU1：RTX 3090，利用率约 `25%`，显存 `4702 / 24576 MiB`，温度 `64°C`
- CPU：36 核，负载约 `75.60 / 69.08 / 64.68`
- 最近 30 分钟新增 `36` 个 `metrics.json`
- 当前在线评估：`active_medium=6`、`active_rep=1`、`all_eval=7`
- 当前无近期关键错误：未发现 OOM、Traceback、Killed、No module、No such file 等。

当前 watcher：

| watcher | PID | 作用 |
|---|---:|---|
| `/data/run_medium_odtrack_family_gpu1_now.sh` | 77737 | ODTrack family medium 队列 |
| `/data/run_ft_ep1_4_representative_completion.sh` | 113912 | 补齐 `ft_ep1-4` representative |
| `/data/run_v4_medium_after_representative_20260825.sh` | 116099 | `ft_v4_ep4/ep5` medium 队列 |

当前在线任务：

| 方法 | 类型 | 进度 |
|---|---|---:|
| `ft_ep8` | medium | 27/30 |
| `ft_ep7` | medium | 27/30 |
| `ft_v4_ep1` | medium | 17/30 |
| `ft_ep6` | medium | 13/30 |
| `ft_ep2` | representative | 5/9 |
| `ft_v4_ep4` | medium | 4/30 |
| `ft_v4_ep5` | medium | 4/30 |

## 5. 已完成 medium 结果

完整 30/30 medium 结果如下。FPS 是并发验证时的观测值；接近阈值的方案后续必须 solo 复测。

| 方案 | AUC | SR | FPS | 判断 |
|---|---:|---:|---:|---|
| `sutrack_b224` | 0.4919 | 0.5416 | 26.0 | 当前完整 medium 精度最稳，sim/hard 专家价值高 |
| `lorat` | 0.4725 | 0.5424 | 8.7 | representative 很强，但 medium 速度慢、泛化掉；保留为真实场景专家参考 |
| `sutrack_t224` | 0.4614 | 0.5070 | 39.5 | 速度达标，是快路径核心候选 |
| `odtrack_t1` | 0.4599 | 0.5130 | 14.2 | 轻量 real-scene 候选，但 medium 速度/精度都不够 |
| `ft_ep5` | 0.4585 | 0.5018 | 13.7 | sim AUC 相对较好，可保留专家分析 |
| `uetrack` | 0.4465 | 0.4815 | 38.5 | 速度达标，是快路径/回退分支候选 |
| `lightfc` | 0.3075 | 0.3135 | 17.1 | medium 表现弱，仅 watchlist，不宜直接投入 |

当前没有完整 medium 方案满足最终提交指标。

## 6. 当前 partial medium 结果

这些结果只能看趋势，不能作为最终排名。

| 方案 | 进度 | AUC | SR | FPS | 备注 |
|---|---:|---:|---:|---:|---|
| `ft_v4_ep4` | 4/30 | 0.5357 | 0.6617 | 13.3 | 前 4 条真实序列，不足以下结论 |
| `ft_v4_ep5` | 4/30 | 0.5211 | 0.6471 | 11.5 | 与 ep4 类似，等 full medium |
| `ft_ep7` | 27/30 | 0.4573 | 0.5088 | 14.1 | 接近完成，保留到完整评估 |
| `ft_v4_ep1` | 17/30 | 0.4444 | 0.4820 | 14.9 | 前期代表性较好，medium 逐步回落 |
| `ft_ep8` | 27/30 | 0.4397 | 0.4864 | 13.4 | 接近完成，代表性里有速度基线价值 |
| `ft_ep6` | 13/30 | 0.4316 | 0.4942 | 14.7 | 曾在少量真实序列上高，但 sim/hard 拉低 |

## 7. representative potential 矩阵要点

代表性 9 序列筛选的作用是识别“组件潜力”，不是最终提交 gate。

当前代表性矩阵中的 core/watchlist：

| 方案 | representative AUC | representative SR | representative FPS | 定位 |
|---|---:|---:|---:|---|
| `lorat` | 0.6021 | 0.7275 | 10.2 | 精度/真实场景专家 |
| `sutrack_b224` | 0.5919 | 0.6899 | 27.6 | 稳定精度核心，sim/hard 专家 |
| `sutrack_t224` | 0.5632 | 0.6579 | 40.5 | 快路径核心 |
| `ft_v4_ep4` | 0.5479 | 0.6627 | 16.7 | sim/hard 专家候选 |
| `ft_v4_ep1` | 0.5396 | 0.6535 | 19.8 | sim/hard 专家候选 |
| `ft_ep8` | 0.5389 | 0.6586 | 27.1 | 速度基线 peer |
| `ft_ep7` | 0.5365 | 0.6513 | 26.4 | 速度基线 peer |
| `ft_v4_ep5` | 0.5339 | 0.6470 | 15.2 | 有单序列胜出 |
| `ft_ep6` | 0.5335 | 0.6474 | 26.7 | 速度基线 peer / sim-hard 候选 |
| `odtrack_t1` | 0.5274 | 0.6222 | 29.5 | 轻量真实场景候选 |
| `uetrack` | 0.5229 | 0.6007 | 51.6 | 快路径/轻量候选 |
| `ft_ep5` | 0.5226 | 0.6356 | 27.0 | 速度基线 peer |
| `lightfc` | 0.4312 | 0.5112 | 28.8 | watchlist，仅供轻量参考 |

仍未补齐 representative 的方法：

- `ft_ep2`: 当前 5/9
- `ft_ep3`: 0/9
- `ft_ep4`: 0/9

注意：`potential_matrix.md` 里还显示旧的 incomplete_probe 行（例如 `ft_ep2` 5/9、`ft_ep3` 4/9、`ft_ep4` 4/9），但当前最新 full9 补跑目录里 `ft_ep3/4` 还是 0/9；必须等 watcher 补齐后再刷新矩阵。

## 8. 当前 gate 状态

`component_diagnostics.md` 最新 gate：

- `NO_SUBMISSION_CANDIDATE`: 当前没有完整方案满足 `AUC>0.80, SR>0.80, FPS>30.0`
- `REPRESENTATIVE_GATE=HOLD`: 还要补齐 `ft_ep2, ft_ep3, ft_ep4`
- `FUSION_GATE=HOLD`: medium validation 未完成，暂不启动最终融合
- `SPEED_GATE=PROVISIONAL`: 候选方案必须 solo FPS 复测，不能用并发 FPS 直接下结论

当前 30 条 medium 上的 per-sequence oracle 约：

- AUC `0.5769`
- SR `0.6609`
- component FPS mean `15.6`

这说明“按已知方法逐序列选最优”的上限仍未接近 0.8，所以后续融合需要真实技术增益，不能只是简单 oracle/投票。

## 9. 当前最有潜力的技术方向

### 9.1 稳定精度核心：`sutrack_b224`

这是当前 full medium 里最稳的精度方案，AUC/SR 最高。缺点是 FPS 只有 26 左右，未达 30。后续方向：

- 做 solo FPS 复测，确认并发是否低估；
- 研究是否可通过输入尺寸、ONNX/TensorRT、batch/预处理优化把速度拉过 30；
- 若不能单独提交，作为融合中的精度专家。

### 9.2 快路径核心：`sutrack_t224` 与 `uetrack`

两者 full medium FPS 均超过 30：

- `sutrack_t224`: FPS 39.5，AUC/SR 高于 `uetrack`
- `uetrack`: FPS 38.5，极快、可作为 fallback

它们精度远不够直接提交，但适合做：

- 默认快 tracker；
- 低风险序列走快路径；
- 高风险或低置信时切精度专家；
- 保证融合方案整体 FPS > 30。

### 9.3 真实场景/高精度专家：`lorat`

`lorat` 在 representative 上是最强精度，但 medium 速度太慢，泛化也不如预期。它不适合作为主干提交，但仍有价值：

- 分析其成功的真实场景序列特征；
- 作为“什么时候需要重定位/慢专家”的启发；
- 若融合只在少数低置信帧调用慢专家，可能有价值，但必须严控调用率。

### 9.4 ODTrack 微调族：`ft_ep6/7/8`、`ft_v4_ep4/5`

这些还没完全跑完，不能丢：

- `ft_ep7/8` medium 已 27/30，接近完整；
- `ft_ep6` 13/30，真实序列曾有高点，但 sim/hard 拉低；
- `ft_v4_ep4/5` 刚到 4/30，前几条真实序列表现尚可，但样本太小；
- representative 上 `ft_v4_ep4/5` 有 sim/hard 专家价值，应等 full medium。

### 9.5 低优先级

- `lightfc`: medium 太弱，只保留轻量参考。
- `direct_erp`、`odtrack_recapture`、`odtrack_recapture_ft_ep7`: representative 已 drop；除非后续融合需要特定机制，否则不要优先投入。

## 10. 已做过的重要调度/脚本改动

这些改动是为了让服务器充分利用，但不破坏当前训练。

1. `run_ft_ep1_4_representative_completion.sh`
   - 原先过于保守，等待 active medium `< 6` 才补 representative。
   - 已改为 `MAX_MEDIUM=7`，允许 6 条 medium + 1 条 representative 并行。
   - 已重启 watcher。
   - 备份示例：`/data/run_ft_ep1_4_representative_completion.sh.bak_max6_20260825_231222`

2. `run_v4_medium_after_representative_20260825.sh`
   - 已修复 `rep_queue_done()` 判断逻辑：不只依赖日志里的 done 文本，而是看实际 checkpoint/metrics 完成情况。
   - 已把 CPU 负载安全线从 `cores * 1.9` 放宽到 `cores * 2.5`，避免 GPU 有空槽但 v4 medium 不启动。
   - 已重启 watcher，并成功启动 `ft_v4_ep4`、`ft_v4_ep5`。
   - 备份示例：`/data/run_v4_medium_after_representative_20260825.sh.bak_safe19_20260825_232047`

3. `medium_component_diagnostics.py`
   - 用于生成 component 诊断、oracle、hard sequences、gate 状态。
   - 路径：`/data/medium_component_diagnostics.py`

4. `summarize_medium_validation.py`
   - 用于刷新 medium 汇总。
   - 路径：`/data/summarize_medium_validation.py`

## 11. 下一任接手后的建议操作顺序

### 第一步：不要打断当前进程，先看状态

运行紧凑审计，确认进度、错误和 gate：

```bash
cd /data/pano360
last_seq=$(tail -1 /data/runs/representative_leaderboard_20260825/medium_validation_sequences.txt 2>/dev/null || true)
nvidia-smi
ps -eo pid,etime,cmd | grep -E 'scripts/eval_official.py.*(runs/medium_validation_20260825|representative_full9)' | grep -v grep
for m in sutrack_b224 sutrack_t224 uetrack odtrack_t1 lightfc lorat ft_ep5 ft_ep6 ft_ep7 ft_ep8 ft_v4_ep1 ft_v4_ep4 ft_v4_ep5; do
  d=/data/runs/medium_validation_20260825/$m
  [ -d "$d" ] && echo "$m $(find "$d" -name metrics.json | wc -l)/30"
done
for e in 1 2 3 4; do
  d=$(printf "/data/runs/finetune_headonly_v3_valid_20260825/ep%04d_representative_full9" "$e")
  echo "ft_ep${e}_rep $(find "$d" -name metrics.json 2>/dev/null | wc -l)/9"
done
/home/wuyou/grt_env/bin/python /data/summarize_medium_validation.py
/home/wuyou/grt_env/bin/python /data/medium_component_diagnostics.py
grep -A12 -n '## Gate status' /data/runs/medium_validation_20260825/component_diagnostics.md
```

### 第二步：等 `ft_ep2` representative 完成

当前 `ft_ep2` 已 5/9。完成后 watcher 应该自动：

1. ingest `ft_ep2`
2. 开始 `ft_ep3` representative
3. 完成后开始 `ft_ep4` representative

如果 `ft_ep2` 完成后 watcher 没有继续启动 `ft_ep3`，检查：

- `/data/ft_ep1_4_representative_completion_20260825.log`
- `/data/run_ft_ep1_4_representative_completion.sh`
- active medium 是否长期超过 7
- load 是否超过安全阈值

必要时只重启 watcher，不要杀正在跑的 eval：

```bash
pgrep -af '^bash /data/run_ft_ep1_4_representative_completion.sh$|^/bin/bash /data/run_ft_ep1_4_representative_completion.sh$'
nohup bash /data/run_ft_ep1_4_representative_completion.sh >>/data/ft_ep1_4_representative_completion_nohup_20260825.log 2>&1 &
```

### 第三步：等当前 medium 补齐

当前还缺：

- `ft_ep6`: 13/30
- `ft_ep7`: 27/30
- `ft_ep8`: 27/30
- `ft_v4_ep1`: 17/30
- `ft_v4_ep4`: 4/30
- `ft_v4_ep5`: 4/30

`ft_ep7/8` 应该最先完成；`ft_v4_ep4/5` 还早。

如果 active medium 掉到 5 以下且还有 pending medium 没启动，检查：

- `/data/run_medium_odtrack_family_gpu1_now.sh`
- `/data/run_v4_medium_after_representative_20260825.sh`
- `/data/medium_odtrack_family_gpu1_now_20260825.log`
- `/data/v4_medium_after_representative_20260825.log`

### 第四步：代表性 gate 清掉后，决定是否给 `ft_ep2/3/4` 开 medium

原则：

- 如果 `ft_ep2/3/4` representative 进入 `core_keep` 或 `watchlist`，应补 medium 30/30；
- 如果只是略低但接近当前速度/精度基线，仍建议至少跑 medium，不要一刀切；
- 如果被明确 drop 且无组件价值，再不投入 full medium。

### 第五步：medium 覆盖完整后才进入融合/自研

融合不是简单取 oracle。当前 oracle 上限还不够高，后续应考虑真正的技术方案：

- 快 tracker 主干：`sutrack_t224` 或 `uetrack`
- 精度专家：`sutrack_b224`，必要时参考 `lorat` 或 ODTrack v4
- 触发机制：基于置信度、目标丢失、运动突变、边界/环绕异常，而不是训练集序列 ID
- 速度约束：慢专家只能低频调用；最终必须 solo FPS > 30
- 360 特性：注意水平环绕、球面 IoU、目标跨边界、长宽比例异常

### 第六步：任何候选接近提交前必须 solo 验证

至少做：

- medium/full validation AUC/SR
- solo FPS 复测
- docker 本地 smoke test
- arena protocol 自测
- 确认 Blackwell/RTX 5090 兼容的 torch/cu128 镜像

然后再问用户是否允许 `docker push`。

## 12. 当前不建议做的事

- 不要现在开始最终融合：证据还没齐。
- 不要因为 `ft_v4_ep4/5` 前 4 条真实序列表现还行就下结论：它们还没进入 sim/hard 段。
- 不要因为 `sutrack_t224/uetrack` 精度低就丢：它们是速度支架。
- 不要因为 `lorat` medium 整体掉了就完全丢：它仍有真实场景专家价值。
- 不要在并发评估时用 FPS 做最终速度判定。
- 不要 push 镜像。

## 13. 当前自动监控策略

本地 automation：

- ID：`grt-360-training-monitor`
- 类型：heartbeat
- 策略：默认静默，只在阶段完成、错误、资源闲置需修复、或需要用户确认时通知
- 当前频率：约 2 小时一次

用户明确不希望“几分钟一汇报”的巡逻式输出。接手者应继续保持事件驱动汇报。

## 14. 最后结论

当前最大事实是：单模型没有接近最终指标，full medium 最强 `sutrack_b224` 也只有 `AUC 0.4919 / SR 0.5416 / FPS 26.0`。但调研方案还没跑完，尤其 `ft_ep2/3/4` representative 与 `ft_v4_ep4/5` medium 仍在进行，因此还不能做最终技术定论。

正确路线是：

1. 等当前 6 medium + 1 representative 跑完；
2. 补齐 `ft_ep2/3/4` representative；
3. 对仍有潜力的 `ft_ep2/3/4` 补 medium；
4. medium 证据完整后，做快路径 + 精度专家 + 置信触发的自研融合；
5. solo FPS 和 AUC/SR 全部过线后，才考虑提交。

## 15. 增量更新（2026-08-25 23:43 CST）

最新一轮远端审计显示任务仍健康运行：

- 当前在线：`6` 条 medium + `1` 条 representative。
- 最近 20 分钟新增 `21` 个结果文件。
- 无 OOM、Traceback、Killed 等近期关键错误。
- `ft_ep2` representative 已完成，并已被刷新进 potential 矩阵。
- `ft_ep3` representative 已自动启动。

`ft_ep2` full9 representative 结果：

| 方案 | n | AUC | SR | FPS | 矩阵状态 |
|---|---:|---:|---:|---:|---|
| `ft_ep2` | 9 | 0.5123 | 0.5872 | 12.4 | drop |

判断：`ft_ep2` 没有显示出明确组件价值，且速度明显低于 `ft_ep5/6/7/8` 的 representative 水平；除非后续策略需要完整微调谱系对照，否则不建议优先给 `ft_ep2` 开 full medium。

当前仍需等待：

- `ft_ep3` representative 完成；
- `ft_ep4` representative 完成；
- `ft_ep6/7/8` medium 完成；
- `ft_v4_ep1/4/5` medium 完成。

当前 gate 仍为：

- `NO_SUBMISSION_CANDIDATE`
- `REPRESENTATIVE_GATE=HOLD: ft_ep3, ft_ep4`
- `FUSION_GATE=HOLD`
- `SPEED_GATE=PROVISIONAL`
